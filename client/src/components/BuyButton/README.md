# BuyButton

## Usage
You can use it in your code like a React component with the jsx tag:

```jsx
<BuyButton id={id} /> 
```

| Prop name | Type | Description                       
| - | - | - | - |
| `id` | `String` | Item ID     